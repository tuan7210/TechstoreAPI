#!/usr/bin/env python3
"""
This repository is now RAG-only.
LoRA adapter inference is disabled. Use the .NET ChatController with retrieval context.
"""
import sys

if __name__ == "__main__":
    print("[RAG-only] LoRA inference is disabled. Use retrieval service + ChatController.")
    sys.exit(0)
